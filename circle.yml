## Customize the test machine
test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/
    - export TC_NUM=10000 && lein do clean, compile :all, midje :config .midje_ci.clj
    - cp target/surefire-reports/* $CIRCLE_TEST_REPORTS/
